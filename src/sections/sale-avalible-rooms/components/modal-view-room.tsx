import Modal, { ModalProps } from "@/components/_cms/ui/modal/modal";
import React from "react";

export default function ModalViewRoom({ ...props }: ModalProps) {
  return <Modal {...props}></Modal>;
}
